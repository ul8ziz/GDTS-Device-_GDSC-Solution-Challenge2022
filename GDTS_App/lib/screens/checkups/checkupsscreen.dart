import 'package:flutter/material.dart';

class CheckupsScreen extends StatelessWidget {
  const CheckupsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('CheckupsScreen');
  }
}
